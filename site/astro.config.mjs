import { defineConfig } from 'astro/config';

// https://astro.build
export default defineConfig({
  site: 'https://portfolio-michael-farias.vercel.app',
  // Sem integrações por ora: site estático puro (HTML/CSS + JS mínimo).
});
